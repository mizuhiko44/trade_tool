# Root-level convenience file.
# If you run from repo root, copy this to .env.
# (The same key is also documented in fx_pattern_tool/.env.example)
ALPHAVANTAGE_API_KEY=your_alpha_vantage_api_key_here
